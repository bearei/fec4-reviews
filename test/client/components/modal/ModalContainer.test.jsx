import React from 'react';
import { shallow } from 'enzyme';
import sinon from 'sinon';
import ModalContainer from '../../../../client/components/modal/ModalContainer';

describe('<ModalContainer />', () => {
  test('should exist', () => {
    expect(ModalContainer).toBeDefined();
  });
});
